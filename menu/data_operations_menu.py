# Imports: Utilities
from utils.console_utils import *
from utils.input_utils import *
from utils.settings import *

# Imports: Database Manager
from db_utils.database_manager import *

# [ METHOD ]: Insert # of table rows
def insert_rows_menu():
    # display current table
    press_to_continue()

# [ METHOD ]: Display all rows in currently selected table
def view_all_rows_menu():
    press_to_continue()

# [ METHOD ]: Display filtered rows (based on selection) in currently selected table
def view_rows_with_filter_menu():
    press_to_continue()

# [ METHOD ]: Update table rows in currently selected table
def update_rows_menu():
    press_to_continue()

# [ METHOD ]: Delete table rows in currently selected table
def delete_rows_menu():
    press_to_continue()